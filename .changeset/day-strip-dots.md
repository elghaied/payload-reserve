---
'payload-reserve': patch
---

Mobile week view: the day-strip chips now look like buttons (surface + border) and show the same status dots as the month grid — one per booking (up to three, then `+N`), a grey dot for an external busy interval — with the booking count in each chip's accessible name.
